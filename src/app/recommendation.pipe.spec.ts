import { RecommendationPipe } from './recommendation.pipe';

describe('RecommendationPipe', () => {
  it('create an instance', () => {
    const pipe = new RecommendationPipe();
    expect(pipe).toBeTruthy();
  });
});
