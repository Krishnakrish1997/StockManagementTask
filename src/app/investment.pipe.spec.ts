import { InvestmentPipe } from './investment.pipe';

describe('InvestmentPipe', () => {
  it('create an instance', () => {
    const pipe = new InvestmentPipe();
    expect(pipe).toBeTruthy();
  });
});
